apiVersion: v2
name: br-engine-service
description: Shared deployment topology for a br-service-engine service — ops contract v1 written once, consumed by a thin per-service chart. The chart versions its ops contract on its own version line, independent of the engine version; a compatibility matrix kept by the deploying platform pairs each chart version with the engine versions it serves.
type: library
version: 1.1.1
home: https://github.com/BotResources/br-service-engine
sources:
  - https://github.com/BotResources/br-service-engine
maintainers:
  - name: BotResources
    url: https://github.com/BotResources
keywords:
  - service-engine
  - deployment
  - topology
  - library
